os:
  - linux
  - osx
sudo: required
language: node_js
node_js:
  - "0.10"
  - "0.12"
  - "4"
  - "6"
before_install:
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew update; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew install gcc48; fi

before_script:
  - npm install winston@2.3.0
  - npm install https://github.com/ioBroker/ioBroker.js-controller/tarball/master --production
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then sudo curl -L http://xrl.us/installperlosx | bash; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then sudo cpan install Bundle::CPAN; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then sudo cpan install Device::SerialPort; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then sudo cpan install IO::Socket::SSL; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then sudo cpan install Net::Telnet; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then sudo cpan install Net::WebSocket::Server; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then sudo cpan install XML::Simple; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then wget http://fhem.de/fhem-5.7.tar.gz; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then tar -xvzf fhem-5.7.tar.gz; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then cd fhem-5.7; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then pwd; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then ls -la; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then echo "define telnetPort telnet 7072 global" >> fhem.cfg; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then sudo make install; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then ls -la /opt/fhem/; fi
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then perl /opt/fhem/fhem.pl /opt/fhem/fhem.cfg; fi
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then wget --no-check-certificate -q https://debian.fhem.de/archive.key | sudo apt-key add -; fi
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then source /etc/lsb-release; fi
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then echo "deb https://debian.fhem.de/stable ./" | sudo tee -a /etc/apt/sources.list; fi
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then sudo apt-get update -qq; fi
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then sudo apt-get install fhem -y; fi
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then sudo echo "define telnetPort telnet 7072 global" >> /opt/fhem/fhem.cfg; fi

env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-4.8
